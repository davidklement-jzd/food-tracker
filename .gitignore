# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local data dumps (USDA, OFF, translation outputs)
data/*
# Výjimka: ručně sepsaná fastfood databáze (malá, nereprodukovatelná)
!data/fastfood-cz.jsonl
# Výjimka: curated seznam českých klasik (vstup pro fetch-kt-foods.mjs)
!data/cz-classics.tsv
# Výjimka: ručně sepsaná databáze alkoholu a drinků
!data/alcohol-cz.jsonl
# Výjimka: ručně sepsaná databáze sladkého pečiva, zákusků, dortů a dezertů
!data/sweets-cz.jsonl

# Temp výstupy skriptů (SQL patche, logy)
tmp/

# Supabase CLI local state
supabase/.temp/

# Claude Code local state
.claude/
